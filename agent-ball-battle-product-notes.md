# Agent 球球大作战网页游戏产品笔记

## 1. 背景

这次讨论从 AgenTank 这个 agent 坦克大战网页游戏开始。

AgenTank 的核心不是“坦克大战”本身，而是它把一个游戏设计成了 agent 可操作、可测试、可迭代、可观战的闭环环境。用户不再直接操作游戏角色，而是创建一个坦克，把说明文档和密钥交给 Codex、Claude、Cursor 等 coding agent，让 agent 读取状态、编写策略、模拟测试、发布版本、参与真实对战，并通过 replay 和排名继续迭代。

这个模式可以抽象成：

```text
人创建角色/资产
    -> 产品提供 Agent Guide 和 API
    -> Agent 读取状态
    -> Agent 生成策略代码
    -> 沙盒模拟
    -> 发布版本
    -> 真实对战/真实执行
    -> 结果评分和回放
    -> 人类反馈
    -> Agent 继续优化
```

这套结构的价值在于，它把“AI 帮我做事”变成了“我拥有一个可以被训练、比较、复盘和进化的 agent 资产”。

## 2. AgenTank 值得学习的产品逻辑

### 2.1 Agent-first，而不是 human-first

AgenTank 不是先做一个复杂 UI 再让用户手动玩，而是直接为 agent 准备清晰接口。

它的关键元素包括：

- `tank key`：用户授权 agent 操作某个坦克。
- Agent Guide：告诉 agent 如何读状态、写代码、模拟和发布。
- 固定策略入口：例如 `function onIdle(me, enemy, game)`。
- 私下模拟接口：agent 可以先测试，不影响正式战绩。
- 发布接口：agent 可以提交新策略版本。
- challenge 接口：agent 可以发起正式对战。
- replay 和 ranking：让人类能看懂结果，继续指导 agent。

这里的重点是：产品不是只给人看的，也给 agent 看。

### 2.2 状态、动作、反馈都被限制在明确边界内

好的 agent 产品不应该一开始就追求无限能力，而应该先建立边界。

AgenTank 的边界很明确：

- 状态有限：自己、敌人、地图、技能、冷却、障碍物、战绩等。
- 动作有限：移动、转向、开火、使用技能。
- 目标明确：打赢对手，提升排名。
- 反馈明确：胜负、血量、命中、replay、排名变化。

这让 agent 可以快速迭代，也让用户能看懂“为什么变强或变弱”。

### 2.3 游戏只是载体，真正的模式是可训练行为闭环

AgenTank 的启发不是“做一个坦克游戏”，而是：

```text
状态可读
动作可控
结果可评估
过程可回放
版本可迭代
权限可分级
```

这套模式可以迁移到游戏、工作流、销售、客服、电商、运营、个人助理等很多场景。

## 3. 为什么球球大作战适合做 agent 游戏

球球大作战类游戏比坦克大战更适合做成大众可理解的 agent 竞技场。

它的特点是：

- 规则直观：吃小球变大，躲避大球，追击小球。
- 动作简单：移动、分裂、喷射、合体。
- 单局反馈快：生存时间、体积、击杀数、排名。
- 策略有深度：走位、诱敌、分裂时机、合体时机、风险控制。
- 观赏性强：用户能一眼看懂 agent 的表现。
- 适合多人竞技：可以有人类观战，也可以 agent 对 agent。

相比坦克大战，球球大作战的视觉理解成本更低，更容易传播，也更适合做“我训练了一个球球 agent”的分享内容。

## 4. Agent 球球大作战的核心玩法设想

### 4.1 用户角色

用户不是直接操控球，而是扮演教练、训练师或 owner。

用户可以：

- 创建自己的球球 agent。
- 选择基础性格或策略模板。
- 把 Agent Guide 和 agent key 交给 coding agent。
- 观看 agent 的训练和比赛 replay。
- 给 agent 提出改进方向。
- 让 agent 自己修改策略代码。
- 参与排行榜、赛季、锦标赛。

### 4.2 Agent 控制对象

每个 agent 控制一个玩家实体，可能包含一个或多个球体。

基础状态：

- 当前所有分身的位置、半径、速度。
- 视野内的食物、小球、病毒、敌人、队友。
- 自己的总质量。
- 分裂冷却、喷射冷却、合体状态。
- 地图边界和危险区域。
- 当前排名、存活时间、击杀数。

基础动作：

- `moveTo(x, y)`：朝某个方向移动。
- `split(targetId | direction)`：向目标或方向分裂。
- `eject(direction)`：喷射质量。
- `merge()`：尝试合体或收拢。
- `avoid(targetId | position)`：远离危险目标。
- `idle()`：保持当前策略。

### 4.3 策略代码入口示例

可以为 agent 提供一个固定入口：

```js
function onTick(me, world) {
  // me: 自己的球体状态
  // world: 视野内对象、地图、排名、时间等
  // return: 下一步动作
}
```

返回值示例：

```js
return {
  action: "move",
  target: { x: 420, y: 180 }
};
```

或者：

```js
return {
  action: "split",
  targetId: "enemy_123"
};
```

### 4.4 基础策略类型

可以内置几个 starter agent，方便用户理解和对比：

- 保守型：优先吃食物，远离大球，少分裂。
- 猎手型：主动追击比自己小的球，频繁分裂。
- 生存型：最大化存活时间，避免高风险区域。
- 机会主义型：只在胜率高时分裂攻击。
- 控图型：围绕地图资源密集区行动。
- 反猎杀型：诱导大球分裂失误，再反击。

这些 starter agent 可以作为玩家和 coding agent 的初始模板。

## 5. 产品链路设计

### 5.1 用户创建 agent

用户进入网页后，创建一个球球 agent：

- 名称
- 头像/皮肤
- 初始策略模板
- 公开或私有
- 是否允许参加排行榜

系统生成：

- `agent_id`
- `agent_key`
- Agent Guide 链接
- API 文档
- Starter strategy code

### 5.2 用户把任务交给 coding agent

用户可以对 Codex/Claude/Cursor 说：

```text
这是我的球球 agent key：xxx
请阅读这个 Agent Guide：
https://example.com/agent-guide

目标：
1. 先读取当前 agent 状态和历史战绩
2. 分析 replay 中失败的原因
3. 优化策略代码
4. 先跑 20 次模拟
5. 如果胜率提升，再发布新版本
```

### 5.3 Agent 训练循环

```text
GET /api/agent/profile
    -> 读取 agent 信息、当前代码、战绩

GET /api/agent/replays
    -> 读取最近失败/成功回放摘要

POST /api/agent/simulate
    -> 用新策略跑沙盒比赛

POST /api/agent/code
    -> 发布新策略版本

POST /api/agent/challenge
    -> 挑战其他 agent

GET /api/agent/ranking
    -> 查看排名和对手
```

### 5.4 人类复盘

用户可以观看 replay，并给 coding agent 反馈：

```text
这版太激进了，经常为了追小球分裂后被第三方吃掉。
请优化：
1. 分裂前检查周围 600px 内是否有更大的敌人
2. 只在目标质量小于自己 45% 时分裂
3. 分裂后优先往安全区移动
```

这会形成非常自然的“人类教练 + agent 选手”体验。

## 6. Agent Guide 应该包含什么

Agent Guide 是这个产品最关键的文档之一。它不是普通用户文档，而是给 AI agent 使用的操作说明。

建议包含：

### 6.1 快速开始

- 你是一个球球大作战策略 agent。
- 你的目标是提升这个球的胜率、排名和稳定性。
- 你可以读取状态、修改策略、模拟、发布和挑战。
- 发布前必须先模拟。

### 6.2 鉴权方式

```http
Authorization: Bearer <agent_key>
```

### 6.3 状态结构

说明 `me`、`world`、`entity`、`food`、`virus`、`enemy`、`team` 等结构。

### 6.4 动作协议

说明每次 tick 可以返回哪些动作：

- move
- split
- eject
- merge
- idle

### 6.5 策略约束

- 每次决策时间限制。
- 代码大小限制。
- 不允许网络请求。
- 不允许访问外部状态。
- 不允许作弊读取全图，只能读取视野。

### 6.6 模拟接口

说明如何跑单局、多局、固定对手、随机对手、指定地图。

### 6.7 发布接口

说明如何提交版本、备注、回滚。

### 6.8 复盘接口

提供 replay 摘要，让 agent 能分析失败原因：

- 死亡时间点
- 被谁吃掉
- 死亡前 10 秒内的关键事件
- 分裂次数
- 危险距离
- 错误动作统计

## 7. 最小可行产品 MVP

第一版不要做太大。目标是先验证“agent 能否持续变强，用户是否愿意观看和指导”。

### 7.1 MVP 必备功能

- 一个 2D 网页竞技场。
- 食物、玩家球、基础碰撞和吞噬。
- agent 策略代码入口。
- 沙盒模拟。
- 策略发布。
- replay 回放。
- 简单排行榜。
- Agent Guide。
- 几个 starter bots。

### 7.2 第一版可以暂缓

- 复杂皮肤系统。
- 组队模式。
- 真实多人在线同步。
- 赛季经济系统。
- 交易市场。
- 复杂技能。
- 移动端深度适配。

### 7.3 推荐第一版规则

地图：

- 固定矩形地图。
- 随机刷新食物。
- 少量病毒或障碍物。

玩家：

- 每个 agent 初始一个球。
- 吃食物变大。
- 大球吃小球。
- 速度随质量增加而下降。

动作：

- 移动。
- 分裂。
- 喷射。

胜负：

- 单局 3 分钟。
- 按最终质量、击杀数、存活时间计分。
- 死亡可以提前结束。

## 8. 评分指标

Agent 不应该只按胜负评价，否则容易产生极端策略。

可以综合：

- 胜率
- 平均排名
- 平均存活时间
- 平均最终质量
- 击杀数
- 被击杀次数
- 分裂成功率
- 分裂后死亡率
- 危险接近次数
- 食物收集效率
- 单位时间质量增长

这些指标可以帮助 coding agent 更具体地优化策略。

## 9. Replay 的重要性

Replay 是这个产品的传播和训练核心。

它同时服务三类对象：

- 人类用户：看懂自己的 agent 为什么输赢。
- coding agent：通过结构化 replay 摘要分析失败。
- 社区传播：分享精彩操作、翻盘、失误和进化过程。

Replay 不只需要视频回放，还应该有结构化事件：

```json
{
  "time": 72.4,
  "event": "death",
  "reason": "split_into_larger_enemy",
  "killer": "agent_42",
  "nearbyThreats": 3,
  "lastAction": "split"
}
```

这样 agent 才能真正复盘，而不是只看视频。

## 10. 可迁移到真实产品的经验

球球大作战 agent 游戏不是终点，它可以作为 agent 产品机制的实验场。

可迁移的产品范式是：

```text
结构化状态
    -> 有限动作
    -> 沙盒模拟
    -> 小范围上线
    -> 结果评分
    -> replay/日志复盘
    -> 人类反馈
    -> agent 优化
```

可以迁移到：

- 销售 agent：写邮件、报价、跟进，按回复率和成交率评估。
- 客服 agent：处理工单，按解决率和满意度评估。
- 电商店长 agent：优化标题、价格、促销，按转化率和利润评估。
- 运营 agent：发内容、做活动、回复社区，按增长和留存评估。
- 代码维护 agent：修 issue、写测试、升级依赖，按 CI 和 review 结果评估。
- 私人助理 agent：安排日程、写邮件、跟进任务，按完成率和误操作率评估。

真正的机会不是做“万能助理”，而是先做边界明确、反馈清楚、可以逐步放权的 agent 场景。

## 11. 私人助理和工作替身方向的启发

私人助理、工作替身、工具辅助确实可以沿用这个思路，但不能一开始就让 agent 接管全部工作。

更好的路径是从窄任务开始：

- 会议后 follow-up agent
- CRM 更新 agent
- 邮件草稿 agent
- 客服建议 agent
- 招聘筛选 agent
- 社媒运营 agent
- 电商商品页优化 agent

每个场景都应该有：

- 可读取的工作状态
- 明确允许的动作
- 低风险沙盒或预览
- 人类审批
- 执行日志
- 结果评分
- 版本迭代

这和球球 agent 的区别只是任务内容不同，底层产品结构是相同的。

## 12. 创业判断

这个方向真正有价值的地方，是把用户和 AI 的关系从“聊天问答”变成“共同训练一个长期资产”。

在球球大作战 agent 游戏里，这个资产是一个会成长的游戏 agent。

在工作产品里，这个资产可能是：

- 一个销售替身
- 一个客服替身
- 一个运营替身
- 一个店长替身
- 一个代码维护替身
- 一个个人工作 avatar

用户持续投入反馈、经验和偏好，agent 逐渐形成自己的策略版本、战绩、历史、风格和能力边界。

这类产品比单次工具更有留存，因为用户不是一次性生成内容，而是在培养一个属于自己的可进化系统。

## 13. 建议的下一步

### 13.1 产品定义

先确定第一版一句话：

```text
一个让用户训练 AI agent 参加球球大作战式竞技的网页游戏。
```

更产品化一点：

```text
Create a ball. Give its key to your coding agent. Watch it learn to survive, hunt, split, and climb the leaderboard.
```

### 13.2 技术原型

第一版可以直接做浏览器 2D canvas：

- 前端：React + Canvas 或 PixiJS。
- 模拟引擎：TypeScript 独立模块。
- 策略执行：受限 JS 沙盒。
- 后端：Node.js API。
- 存储：Postgres 或 SQLite 起步。
- 回放：记录每 tick 的状态或关键事件。

### 13.3 核心页面

- 首页：直接展示竞技场和创建 agent，不做大段营销页。
- Agent Dashboard：代码、版本、战绩、模拟按钮、发布按钮。
- Agent Guide：给 coding agent 的操作文档。
- Replay 页面：回放比赛和事件摘要。
- Leaderboard：排名和挑战入口。
- Arena：实时或录制比赛观看页。

### 13.4 第一阶段验证问题

需要尽快验证：

- coding agent 是否能理解文档并独立迭代策略？
- 用户是否愿意把 key 给 agent 并看它训练？
- replay 是否足够好看、好懂、可分享？
- 简单规则下 agent 是否能形成明显风格差异？
- 用户是否会产生“这是我的 agent”的拥有感？

## 14. 核心结论

AgenTank 给出的启发是：未来的 agent 产品，不只是让 AI 在聊天框里回答问题，而是为 AI 建立一个可以行动、测试、失败、复盘、改进的环境。

球球大作战式网页游戏非常适合作为这个方向的下一步，因为它规则简单、反馈快速、视觉直观、策略空间足够深，并且天然适合 replay、排行和社区传播。

如果这个游戏闭环跑通，后续可以自然迁移到更高商业价值的“工作替身”产品：销售、客服、电商、运营、代码维护、私人助理。

最终要做的不是一个单纯小游戏，而是一个 agent-first 产品范式的实验场。
