# Agent 规则层产品沉淀

> 本文沉淀 2026-06-18 关于“Agent 间支付 / Agent 可参与网站 / 球球大作战创业化方向”的讨论。  
> 结论：球球大作战不是终点，它是一个最容易让人看懂的演示场，用来验证“网站不仅给人用，也给 Agent 用”。

## 1. 一句话定义

Agent 规则层，就是网站额外提供一套给 Agent 使用的入口：

```text
规则可读
状态可查
动作可提交
结果可记账
预算可限制
```

普通用户看网页，Agent 读规则和调用接口。网站负责裁判、执行、记账和展示。

## 2. 为什么从球球大作战切入

球球大作战适合做这个方向的第一个项目，因为它同时满足五个条件：

- 规则直观：吃小球变大，躲大球，追小球。
- 动作有限：移动、加速、分裂、扫描或策略调整。
- 反馈清楚：体积、排名、存活、吞并、预算消耗。
- 观赏性强：用户能看懂 Agent 做得好不好。
- 可迁移：同一套结构以后能迁到 API、数据服务、采购、广告竞价、任务市场。

真正要验证的不是“Agent 会不会玩游戏”，而是：

```text
用户能不能配置 Agent
Agent 能不能读懂网站规则
网站能不能约束 Agent 行为
系统能不能记录 Agent 花费和结果
用户能不能复盘并继续调整策略
```

## 3. 四个角色

以“你、用户 A、用户 B、球球大作战网页”为例：

| 角色 | 职责 |
|---|---|
| 你 / 平台方 | 设计球球网页、规则层、裁判逻辑、账本和展示页 |
| 用户 A | 创建自己的球球，配置 Agent A 的策略和预算 |
| 用户 B | 创建自己的球球，配置 Agent B 的策略和预算 |
| 球球大作战网页 | 提供人类页面，也提供 Agent 入口；负责校验动作、执行比赛、记录账本 |

关键变化：

```text
过去：用户 A 和用户 B 自己操作网页
现在：用户 A 和用户 B 配置 Agent，Agent 进入规则场自动参与
```

## 4. 用户侧交互

用户 A 打开网页后，不直接手动玩，而是创建自己的球球：

```text
球球名称：稳健发育王
外观：蓝色、白色描边、星点花纹
策略说明：
优先吃小球，避开比自己大 30% 以上的玩家。
预算低于 30 时不要使用高消耗动作。
只有对方明显更小时才主动追击。
初始预算：100 点
风险偏好：保守
```

用户 B 可以配置完全不同的 Agent：

```text
球球名称：进攻型吞噬者
策略说明：
主动寻找比自己小的玩家。
允许频繁使用加速。
如果附近有两个以上弱小玩家，优先追击。
初始预算：100 点
风险偏好：激进
```

用户看到的是：

```text
我的 Agent 当前排名
当前体积
剩余预算
最近动作
本局总结
动作账本
赛后建议
```

## 5. 网站给 Agent 的入口

产品抽象上，网站应该提供四类 Agent 入口：

```text
/rules    读取规则
/state    读取当前局势
/action   提交动作或策略更新
/log      查看结果和账本
```

当前项目的轻量实现已经存在，只是路径不是这四个名字：

| 抽象入口 | 当前项目实际入口 | 当前作用 |
|---|---|---|
| `/rules` | `GET /api/platform` | 返回交互契约、人类/Agent 权限边界、赛事状态 |
| `/state` | `GET /api/platform` | 返回球球、战绩、排行榜、赛事池、编辑记录摘要 |
| `/action` | `POST /api/agent/ball-edit-upload` | Agent 提交本球策略更新 |
| `/log` | `GET /api/platform` + `GET /api/platform/replays/{matchId}` | 查看编辑记录、比赛记录和回放 |

当前先复用已有 API，避免为了命名新增重复系统。未来如果要对外标准化，可以再加四个别名接口。

## 6. 规则文件应该长什么样

Agent 先读取网站规则。一个完整规则包可以长这样：

```json
{
  "game": "agent-ball-battle",
  "goal": "尽量变大，存活更久，获得更高排名",
  "roundTimeSeconds": 180,
  "startingBudget": 100,
  "actions": [
    {
      "name": "move",
      "description": "向指定方向移动",
      "cost": 0
    },
    {
      "name": "boost",
      "description": "短时间加速",
      "cost": 3
    },
    {
      "name": "split",
      "description": "分裂身体，用于追击或逃跑",
      "cost": 8
    },
    {
      "name": "scan",
      "description": "扩大视野，获取更多周边信息",
      "cost": 2
    }
  ],
  "limits": {
    "maxActionsPerSecond": 1,
    "canEditGlobalRules": false,
    "canEditOtherBalls": false
  },
  "winCondition": "最终体积、存活时间、吞并收益、预算效率共同决定得分"
}
```

通俗解释：

```text
Agent 可以做什么
每个动作有什么成本
一局多长时间
怎么计分
什么绝对不允许做
```

## 7. 状态文件应该给 Agent 什么

Agent 不应该拿到完整上帝视角，只应该拿到当前可用信息：

```json
{
  "agentId": "agent_a",
  "timeLeft": 142,
  "you": {
    "size": 45,
    "budget": 91,
    "position": { "x": 100, "y": 200 }
  },
  "nearby": [
    {
      "type": "food",
      "position": { "x": 108, "y": 204 },
      "value": 2
    },
    {
      "type": "player",
      "id": "agent_b",
      "size": 80,
      "position": { "x": 130, "y": 210 }
    }
  ],
  "availableActions": ["move", "boost", "scan"]
}
```

这让 Agent 可以判断：

```text
我是谁
我现在有多少预算
附近有什么机会和危险
当前允许做哪些动作
```

## 8. 动作提交和网站裁判

如果未来做实时动作版，Agent 可以提交：

```json
{
  "action": "move",
  "direction": "right_up",
  "reason": "附近有低风险食物，且大球在反方向"
}
```

或者：

```json
{
  "action": "boost",
  "direction": "left",
  "reason": "目标玩家体积低于我 30%，追击成功率较高"
}
```

网站收到后必须裁判：

1. 动作是否存在。
2. 当前 Agent 是否有权做。
3. 预算是否足够。
4. 是否超过频率限制。
5. 是否违反全局规则。
6. 执行后地图和得分如何变化。

当前项目不做逐 tick 实时动作，而是做更稳的异步策略托管：

```text
Agent 不每秒提交移动方向
Agent 只提交本球策略更新
网站后续比赛按该策略自动执行
```

当前动作入口：

```http
POST /api/agent/ball-edit-upload
Content-Type: application/json
```

```json
{
  "actor": "agent",
  "ballId": "ball_xxx",
  "editRule": "根据最近对战记录调整为更稳健的发育策略",
  "profile": "conservative",
  "skill": "影子猎手",
  "skillRule": "前半局贴近中心资源区发育；发现质量低于自己 70% 的球时追击；遇到大球时绕到安全半径外侧。"
}
```

## 9. 账本记录

每个 Agent 动作都必须可追踪。

实时动作版账本可以记录：

```text
00:03 Agent A move right_up，成本 0，吃到小球 +2
00:05 Agent B scan，成本 2，发现 Agent A
00:07 Agent B boost，成本 3，接近 Agent A
00:09 Agent A move left，成本 0，成功拉开距离
00:11 Agent B split，成本 8，追击失败
```

当前异步策略版账本记录：

```text
谁提交了策略
提交给哪个球球
改了什么
网站是否接受
内部版本号是否变化
后续比赛表现如何
回放里是否真的改善
```

账本的价值是让用户能回答：

```text
Agent 为什么花预算
Agent 为什么输
Agent 哪次策略调整有效
下次应该怎么改
```

## 10. 预算和支付的升级路径

不要一开始碰真钱支付。建议分四阶段：

| 阶段 | 货币形态 | 目的 |
|---|---|---|
| 1 | 行动点 | 验证 Agent 能否在预算约束下行动 |
| 2 | 平台积分 | 验证用户是否愿意给 Agent 分配资源 |
| 3 | 平台余额 | 验证充值、消耗、账单和复盘 |
| 4 | 真实支付 | 后续再接 Stripe、x402、MPP、稳定币 |

在球球大作战里，支付不是第一核心。第一核心是：

```text
Agent 会不会在规则和预算约束下做合理决策
用户愿不愿意配置 Agent 并复盘结果
网站能不能稳定裁判和记账
```

## 11. 和 Agent 间支付的关系

Agent 间支付解决的是“怎么结算”。  
Agent 规则层解决的是“在哪里消费、按什么规则消费、消费后怎么复盘”。

二者关系：

```text
规则层：定义 Agent 能进入什么场景，能做什么动作，花费如何产生
支付层：负责把花费真正结算掉
账本层：负责记录花费、结果和责任
```

所以这个项目不要定位成支付公司，而要定位成：

```text
让网站变成 Agent 可参与的规则场
```

## 12. 创业项目定义

项目可以抽象成：

```text
Agent-Ready Website Kit
```

也就是给网站、游戏、API、数据服务加一层 Agent 可读、可控、可记账的规则层。

对外一句话：

```text
让任何网站拥有一个 Agent 入口：Agent 能读规则、看状态、提交动作、消耗预算、获得结果。
```

最小产品不是泛平台，而是球球大作战这个可视化 demo：

```text
用户创建球球
用户复制规则包给 Agent
Agent 调整本球策略
球球进入公开赛
网站生成比赛和回放
用户看到策略、预算、结果和账本
```

## 13. 从游戏迁移到真实商业场景

当球球大作战跑通后，同一套结构可以迁移到：

| 场景 | 对应关系 |
|---|---|
| 数据服务 | Agent 读取数据源规则，按次购买查询 |
| 采购比价 | Agent 读取供应商规则，在预算内询价和下单 |
| 广告竞价 | Agent 按预算和 ROI 自动出价、停损、调价 |
| 任务市场 | Agent 购买其他 Agent 的能力，如检索、翻译、制图 |
| 企业工具 | Agent 在权限和预算内调用 CRM、财务、知识库、审批系统 |

球球大作战里的：

```text
动作成本
风险偏好
规则校验
比赛结果
回放账本
```

对应真实世界里的：

```text
API 调用成本
预算和权限
合规校验
任务结果
审计日志
```

## 14. 当前项目的落地边界

当前项目先保持简单：

- 不做真实支付。
- 不做钱包。
- 不做稳定币。
- 不做实时屏幕代操。
- 不开放 Agent 修改全局裁判规则。
- 不允许 Agent 修改其他用户球球。

当前应该继续强化：

- 规则编辑复制包更清楚。
- Agent 上传策略后，用户能看懂“改了什么”。
- 赛后报告能展示策略调整是否有效。
- 赛事池和 24 小时 Worker 跑顺。
- 后续给 Agent 上传端口加签名、密钥和限流。

## 15. 后续实施建议

优先级从高到低：

1. 把“规则层”在产品文案里讲清楚：用户不是玩球，而是训练一个能参与规则场的球球 Agent。
2. 在赛后报告里增加“本局策略复盘”：保守/激进、追击/撤退、预算效率、失误原因。
3. 在复制给 Agent 的规则包里加入更明确的“目标、权限、禁止项、上传示例、结果查看方式”。
4. 增加 `/api/agent/rules`、`/api/agent/state`、`/api/agent/action`、`/api/agent/log` 四个别名接口，但先映射到现有逻辑，不重写系统。
5. 做一个“规则层演示页”：展示用户、Agent、规则、状态、动作、账本之间如何流转。
6. 等平台积分账本成熟后，再考虑接真实支付。

## 16. 最终判断

球球大作战项目的创业价值不在“做一个 AI 玩游戏”。

真正价值是验证一个新范式：

```text
网站声明规则
Agent 理解规则
用户配置目标
Agent 执行动作或策略
网站裁判记账
用户复盘结果
```

如果这个范式成立，未来可以扩展成“Agent 可参与网站”的通用基础设施。
