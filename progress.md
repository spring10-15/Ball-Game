Original prompt: 你参考 agent 坦克大战再设计一下，不要英文，纯中文方便用户理解；随后要求设置开放接口，让不同用户创建并部署自己的球球、组织对局、查看球球状态和战绩，用户可改外观，内部设定与机制由 agent 修改。

## 进度

- 已确认当前项目是 React + Vite 前端，核心模拟器在 `core/simulator.ts`，本地 API 写在 `vite.config.ts`。
- 新需求需要从固定内置智能体扩展为本地平台层：用户、球球、部署状态、对局记录、外观设置、Agent 托管策略。
- 已新增 `core/platform.ts`，本地持久化 `out/platform-state.json`，支持创建球球、改外观、部署、下架、组织对局、记录战绩。
- 已在 Vite 本地服务新增开放接口，用户接口不能修改策略档位；策略档位只能走智能体专用接口。
- 已新增前端“开放大厅”，默认首屏展示用户球球资产、外观工坊、部署入口和对战记录。
- `npm run typecheck` 已通过。
- `npm run build` 已通过。
- 已通过本地接口联调：读取平台状态、创建“紫星球”、部署、组织 20 秒对局，生成 4 个参赛球球和 200 帧回放。
- 已通过项目本地 Playwright 浏览器验证：开放大厅、组织对局、回放、事件复盘、操作手册都能打开；可见文本未发现英文词；桌面和手机无横向溢出。
- 技能自带测试脚本因为从技能目录解析不到项目内 Playwright 包而未能直接运行，已改用项目本地 Playwright 执行同等浏览器验证。
- 平台状态已改存到非公开的 `data/platform-state.json`，回放仍写入 `out/last-replay.json` 供前端读取。
- 已按最新要求收窄用户网页权限：每个用户最多 3 个球球，网页端不再提供部署/下架、组织对局、训练评测入口。
- 网页端当前只暴露：创建/查看球球、编辑名称和样式、对战记录、球球编辑记录、球球排行榜、对战回放。
- 已新增智能体编辑上传端口 `/api/agent/ball-edit-upload`，提交 `actor: "agent"`、`ballId`、`editRule` 后由智能体修改内部托管档位并记录编辑历史。
- 已加入旧数据迁移：加载平台状态时自动把每个用户裁剪到最多 3 个球球，并补齐已有球球的初始编辑记录。
- 已重新生成当前 3 个球球的一局系统对战，确保平台球球数量和最新回放一致。
- 已按 AgenTank 产品笔记调整网页设置：默认页改为“球球资产控制台”，突出“交给智能体”的设置包，而不是网页端直接训练。
- “交给智能体”卡片现在包含球球专属编号、可编辑的球球编辑规则、给智能体的设置包和复制按钮；复制带剪贴板备用路径。
- 已按“首页只留我的球球”的要求重构球球中心：默认只显示“我的球球”列表；创建球球、编辑样式、交给智能体改为球球中心内的二级按钮页签。
- 已清理测试产生的默认“我的球球”数据，当前平台状态回到 3 个示例球球。
- 已重新验证：首页 h2 只包含“球球中心 / 我的球球”，二级页签可切换，无禁用入口泄漏、无横向溢出、无控制台错误。
- 已补强“复制给智能体”的设置包：包含上传端口 `POST /api/agent/ball-edit-upload`、JSON 请求示例、必填字段、可选 `profile` 档位说明和执行要求。
- 已拆出平台纯状态操作，新增 Vercel API 显式路由；共享处理器放在 `server/api-handler.ts`，确保 Hobby 计划下函数数不超过 12 个。
- 已新增 `core/platform-storage.ts`：本地开发写 `data/platform-state.json`，Vercel 生产必须配置 Redis/KV；Redis 写入带短锁，降低并发写状态冲突。
- 已新增 `vercel.json`、`.env.example` 和 `VERCEL_DEPLOYMENT.md`，记录构建、环境变量、开放接口、并发限制和公开上线风险。
- 已补浏览器本地用户 ID：同名用户不再共享 3 个球球额度；“我的球球”只显示当前浏览器用户的球球。
- 已验证 Vercel API handler：读取平台、创建球球、编辑外观、智能体上传、组织 1 秒对局均通过。
- 已验证创建上限：同一用户第 4 个球球返回 400，错误为“每个用户最多只能创建 3 个球球”。
- 已验证生产未配 Redis/KV 时会返回明确中文 500，不会把数据写进无状态函数本地文件。
- 已通过 `npm run typecheck` 和 `npm run build`。
- 已用项目本地 Playwright 验证桌面和手机界面：无控制台错误、无横向溢出；“交给智能体”设置包包含球球 ID 与上传指引。技能自带脚本仍因包解析路径找不到 Playwright，已记录并用项目本地 Playwright 覆盖验证。
- 已新增邮箱验证码登录：`/api/auth/request-code`、`/api/auth/verify-code`、`/api/auth/me`、`/api/auth/logout`。
- 登录成功后服务端写 HttpOnly Cookie；创建球球、编辑外观、组织对局都要求登录，服务端只认 session 用户，不接受前端伪造 `ownerId`。
- 平台快照已隐藏用户邮箱、验证码和 session；邮箱登录生产环境需要 `AUTH_SECRET`、`RESEND_API_KEY`、`AUTH_EMAIL_FROM`。
- 已验证鉴权：未登录创建返回 401；邮箱登录后可创建；另一个邮箱用户编辑该球球返回 403。
- 已验证邮箱登录 UI：本地开发验证码可登录，登录后进入球球中心，无控制台错误、无横向溢出。测试后已还原 `data/platform-state.json`，仍为 3 用户、3 球球、无 auth 临时数据。
- 已按最新反馈重排信息架构：顶层只保留“首页 / 竞赛场 / 个人中心”；首页展示总玩家数、球球总数、球球质量合计、竞赛场入口、个人中心入口、动态图和榜单条形图。
- 已把“编辑记录”移动到个人中心内；竞赛场内嵌“对战记录 / 排行榜 / 对战回放”。
- 已修复创建球球体验：创建成功后显示成功提示，并自动跳转到该球球的“编辑样式”设置页，降低连续误点创建 3 个球球的风险。
- 已修复“查看”无反馈/编辑对象错位：点击球球卡片的“查看”会立即进入对应球球设置页，并使用本地 activeBallId 先行更新，避免父组件异步状态导致编辑对象前后冲突。
- 已把回放速度按钮改为 `1倍 / 2倍 / 4倍 / 8倍 / 16倍`。实测当前模拟器按固定局时结算，不会 3 个玩家提前淘汰到只剩 1 个才结束；60 局、240 秒配置下全部在 240 秒结算，因此用户观看不超过 3 分钟的规则应由局时和倍速共同保证。
- 已用项目本地 Playwright 验证：邮箱登录、首页要素、创建后跳转与提示、两个球球间“查看”切换、个人中心编辑记录、竞赛场回放倍速、桌面/手机无横向溢出、无控制台错误。测试数据已还原到测试前状态。
- 根据浏览器批注移除首页中的“竞赛场：对战、榜单、回放”详情卡；竞赛场只保留在顶部导航入口中。
- 根据浏览器批注移除首页中的“个人中心”摘要卡；个人中心只保留在顶部导航入口中。
- 已把首页压缩为登录后一屏总览：项目总览指标、项目动态图、排行榜走势并排展示；1052x762 视口无纵向滚动、无横向溢出。
- 已处理竞赛场批注：删除标题右侧重复说明；对战记录只展示当前登录用户名下球球的参赛记录；删除重复的“球球对战记录”列表。
- 已处理回放批注：删除事件流模块，删除“危险 / 有效冲刺 / 错误”三个低解释价值指标。
- 已处理个人中心批注：把“球球中心 / 创建球球 / 编辑样式 / 规则编辑 / 编辑记录”统一放到个人中心顶层按钮；删除球球中心内部重复按钮；“交给智能体”改名为“规则编辑”。
- 已重新以普通用户视角跑完整浏览器巡检：邮箱登录、首页、竞赛场三页签、回放页、个人中心五个入口均检查；无控制台错误。测试数据已还原到测试前状态。
- 已处理创建上限批注：当前登录用户达到 3/3 后，个人中心顶层“创建球球”入口会变成“已达上限”并禁用，不能再点击进入创建页。
- 已处理最新个人中心批注：我的球球改为横向三列平铺，不再挤在中间；编辑样式页左侧列出当前用户全部球球，可切换后分别编辑对应外观；规则编辑页移除人工规则输入，只保留球球选择和给智能体复制的只读规则包窗口。
- 已再次通过 `npm run typecheck` 和 `npm run build`。
- 已用项目本地 Playwright 做人类视角巡检：首页 1052x762 无横向/纵向滚动；个人中心创建上限禁用、我的球球三列、编辑样式 3 个球可切换、规则编辑无 textarea 且规则包包含球球专属编号和上传端口；竞赛场和回放页无控制台错误。巡检临时数据已还原，当前仍为 4 用户、6 球球。
- 已给“编辑样式”增加删除球球能力：前端有确认弹窗和红色危险按钮；后端新增 `DELETE /api/platform/balls`，要求邮箱登录且只能删除自己的球球；删除会同步移除该球球、相关编辑记录，并从历史对局结果中清掉对应参赛记录。
- 已验证删除流程：临时用户创建 2 个球球后删除 1 个，编辑列表与球球中心列表都减少到 1 个；控制台无错误；临时数据已还原，当前仍为 4 用户、6 球球。
- 已按批注隐藏“规则编辑”里的长规则包内容：页面只展示当前球球和“复制给智能体”按钮；复制内容仍包含球球专属编号与 `POST /api/agent/ball-edit-upload` 上传指引。已用授权 Playwright 验证页面无可见规则包文本、剪贴板内容完整、控制台无错误；临时数据已还原。
- 已修复 Vercel 部署后 `/last-replay.json` 缺失导致的“Unexpected token 'T' / The page could not be found”问题：前端现在先读静态回放，再尝试 `/api/replay/demo`，最后会在浏览器端直接生成默认回放，避免首屏被回放文件阻断；同时补了线上 `/api/replay/demo`、`/api/run-sim`、`/api/run-eval`。
- 已继续加固线上首屏 JSON 解析：`/eval-summary.json` 不再因错误文本阻断页面，平台 API 客户端会先读文本再安全解析；未登录状态不再预先加载 `/api/platform`，避免平台接口异常导致登录页打不开。已用 Playwright 模拟 `/last-replay.json`、`/eval-summary.json`、`/api/platform` 全部返回 `The page could not be found`，仍能进入邮箱登录页且无 `Unexpected token`。
- 已新增球球“专属技能”能力：智能体上传端口支持 `skill` 字段，当前支持 `none / forage / evade / dash`。技能只包装本球策略决策，如优先觅食、提前避险、谨慎短冲，不修改全局质量、冷却、吞噬判定或其他球球状态。
- 已把“专属技能”从固定选项泛化为自由设定：`skill` 可写自定义技能名称或一句技能设定，`skillRule` 可写触发条件和偏好；系统自动归类到安全行为模型，旧的 `none / forage / evade / dash` 仍兼容。
- 已按“放宽 agent 发挥空间”扩展技能行为模型：新增 `hunt / edge / center / shadow`，分别对应猎手机会、贴边求生、中心控场、影子绕行；前端复制包改为允许描述触发条件、风险阈值、地图偏好、追击/撤退优先级，但仍禁止改全局物理、质量收益、吞噬判定、复活、无敌、冷却和其他球球状态。
- 已检查上线后的智能体复制内容风险：复制包中的上传端口从相对路径 `/api/agent/ball-edit-upload` 改为当前网站完整域名，避免外部智能体不知道上传到哪个站点；同时加入 `fetch` 上传示例。
- 已完成自动开局方案：`GET /api/platform` 会在持久化状态锁内检查已部署球球，达到 3 个自动生成 60 秒对局；同一阵容 5 分钟内最多自动开一局，阵容或内部策略版本变化后可再次开局。
- 已按“人类/Agent 权限严格划分”重构入口：人类网页保留创建球球外观身份、编辑出场名/颜色/花纹、复制给 Agent、观看比赛；删除球球和手动开局接口返回 403；行为、技能、战略、移动方向只由 `POST /api/agent/ball-edit-upload` 接管。首页已改成更直接的开场路径和更强竞技视觉。
- 已新增“球球公开赛”赛事活动：竞赛场可点击参加赛事，按真实邮箱用户每人选 1 个已部署球球，2 位真实用户即可开局；开局后写入完整对战记录并进入回放。平台状态归一化会清理无邮箱种子用户和 `example.com` 测试用户，相关虚拟球球与旧对局同步移除。
- 已把赛事升级为 24 小时连续活动雏形：用户点击参加赛事后进入赛事池，新增 `npm run event-worker` 常驻后台脚本持续开局；每局赛事写入独立 replay 存储，竞赛场战报可点击“观看本局”加载对应回放，回放中球球质量和半径会随吃质点/吞并变化。
- 已参考新的产品讨论，明确当前 Agent 与网站交互采用“异步策略托管”：Agent 不看屏幕、不点按钮、不逐 tick 提交动作，而是读取规则/状态/账本后通过 `POST /api/agent/ball-edit-upload` 提交本球策略更新。
- 已新增 `AGENT_INTERACTION_SPEC.md`，把规则、状态、动作、账本四类入口映射到当前真实 API，并写清人类/Agent 权限边界、动作格式、网站校验和账本记录。
- 已在平台快照新增 `agentContract`，前端“复制给 Agent”页面显示交互契约，复制包也包含四类入口、允许/禁止字段、校验规则和账本记录。
- 已在 `AGENT_GUIDE.md` 和 `design-spec.md` 顶部补充当前轻量落地说明，避免旧的 agent_key/策略代码发布设想与现有实现混淆。
- 已通过 `npm run typecheck` 和 `npm run build`；已用项目本地 Playwright + 系统 Chrome 检查桌面与手机“复制给 Agent”页面，交互契约可见且无横向溢出。技能自带脚本仍因从技能目录解析不到项目内 Playwright 包而无法运行。
- 已沉淀 2026-06-18 关于 Agent 间支付、Agent 可参与网站、球球大作战创业化方向的完整讨论到 `AGENT_RULESPACE_PRODUCT_SPEC.md`：明确“规则可读、状态可查、动作可提交、结果可记账、预算可限制”的 Agent 规则层，并说明它和当前异步策略托管实现、未来支付接入、真实商业场景迁移之间的关系。
- 已修复赛事池补齐逻辑：用户点击参加赛事时会按真实用户每人选 1 个最新部署球球加入赛事池；Worker 开局前如果发现当前赛事池不足，也会从真实用户已部署球球里自动补齐，避免“平台用户够了但赛事池只有 1 人”导致赛事停住。
- 已补线上续赛兜底：Vercel Hobby 不支持每分钟 Cron，因此 `GET /api/platform` 会在读取状态时尝试推进赛事；如果赛事池够人且距离上一局超过 60 秒，会自动开一局并保存回放，避免没有常驻 Worker 时赛事长时间停住。

## 待办

- 公开大规模使用前建议给智能体上传端口加密钥/签名和 IP/用户级限流。
- 后续产品文案和赛后报告可以吸收 `AGENT_RULESPACE_PRODUCT_SPEC.md` 的表达：用户不是手动玩球，而是训练一个能进入规则场、在预算内行动、可复盘的球球 Agent。
- 后续如果要做更强的 Agent 专用入口，可新增 `/api/agent/rules`、`/api/agent/state`、`/api/agent/action`、`/api/agent/log` 四个别名接口；当前先复用现有 API，避免重复系统。
- 24 小时赛事 Worker 需要部署到 Render/Railway/Fly/VPS 等常驻进程环境；Vercel 继续负责网页和 API，不承担 24 小时常驻比赛进程。
- 若未来希望“玩家淘汰后立刻结束”，需要改模拟器结束条件；当前胜负是按局时结算排名。
- 后续可把智能体专用调参入口做成后台工作台，并把技能效果统计加入赛后报告。
- 后续可把对局编组从“全部已部署”升级为房间制或赛季制。
